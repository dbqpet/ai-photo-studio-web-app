# AI background removal / styling (first matching provider wins)
FAL_KEY=
REPLICATE_API_TOKEN=
REMOVE_BG_API_KEY=

# Google Gemini — smart photo pre-validation before generation
GEMINI_API_KEY=
# Optional model override (default: gemini-flash-latest)
GEMINI_MODEL=

# Stripe Checkout ($18 HKD). Leave blank to use mock checkout in local dev.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
